#Find all files in a file. 
$files = Get-ChildItem -Directory | select-object Name

#Stick each file into an array
$UserOptions = @()

foreach ($file in $files)
{
    $UserOptions += $file
}

#Print out avalible files on the screen
$daIndex = 0
Foreach ($Option in $UserOptions) {

Write-Host $UserOptions[$daIndex]
$daIndex += 1
Write-Host $daIndex
} 

#Ask the user to pick one

#Create report of all fles. 
#Get-ChildItem -Recurse